language: ruby

before_install:
 - sudo apt-get install -y asciidoctor

install:
  make

deploy:
  provider: pages
  skip_cleanup: true
  github_token: HhuYZTFJHubh91YLwgc6Mg # Set in travis-ci.org dashboard
  on:
    branch: master
  
addons:
  apt:
    update: true
